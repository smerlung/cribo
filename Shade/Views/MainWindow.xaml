<Window x:Class="Shade.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shade" Height="350" Width="525" WindowState="Maximized">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
        </Style>
    </Window.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top" BorderThickness="1" BorderBrush="Black">
            <StackPanel  Orientation="Horizontal">
                <Button Command="{Binding CommandOpen}">Open</Button>
                <Button Command="{Binding CommandSave}">Save</Button>
                <Button Command="{Binding CommandEncrypt}">Encrypt</Button>
                <Button Command="{Binding CommandDecrypt}">Decrypt</Button>
                <Button Command="{Binding CommandExit}">Exit</Button>
            </StackPanel>
        </Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image Stretch="Uniform" Source="{Binding SelectedImagePath}"></Image>
            <TextBox AcceptsReturn="True" TextWrapping="Wrap" Grid.Column="1" Text="{Binding Data}"></TextBox>
        </Grid>
    </DockPanel>

</Window>
